{
  "name": "reui-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for reui trivia game on Base",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio reui-trivia",
    "create-local": "graph create --node http://localhost:8020/ reui-trivia",
    "remove-local": "graph remove --node http://localhost:8020/ reui-trivia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 reui-trivia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}